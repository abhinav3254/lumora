<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lumora">

        <!-- Splash — this is now the entry point -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main screen — no longer the launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Settings screen shown in live wallpaper picker -->
        <activity
            android:name=".WallpaperSettingsActivity"
            android:exported="true"
            android:label="Lumora Settings">
            <intent-filter>
                <action android:name="android.app.WallpaperManager.SETTINGS" />
            </intent-filter>
        </activity>

        <!-- Live Wallpaper Service -->
        <service
            android:name=".SlideshowWallpaperService"
            android:exported="true"
            android:label="Lumora"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/wallpaper" />
        </service>

    </application>

</manifest>